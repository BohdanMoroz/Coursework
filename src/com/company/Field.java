package com.company;

import javax.swing.*;

public class Field {
    Field(){
//        makeBtn(imageIcon);
    }

    public JButton makeBtn(ImageIcon imageIcon){
        return new JButton(imageIcon);
    }
}
